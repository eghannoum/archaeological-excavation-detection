# ---------------------------------------------------------------------------
# Core dependencies — archaeological hole detection pipeline
#
# NOTE ON TORCH: keep this file CPU-installable. The default PyPI torch build
# supports CUDA via runtime wheels. For a CUDA-pinned build use:
#   pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cu128
# (see requirements-gpu.txt)
# ---------------------------------------------------------------------------

# ---- Core ML framework (portable) ----
torch>=2.4,<2.12
torchvision>=0.19,<0.27

# ---- Detection ----
ultralytics==8.4.72

# ---- Augmentation ----
albumentations>=1.4,<3.0

# ---- Experiment tracking & HPO ----
mlflow>=3.14,<4.0
optuna>=4.9,<5.0

# ---- Config ----
hydra-core>=1.3,<2.0
omegaconf>=2.3,<3.0
PyYAML>=6.0

# ---- Data / numerics ----
numpy>=1.26,<3.0
pandas>=2.2,<3.0
scipy>=1.13,<2.0
scikit-learn>=1.5,<2.0
Pillow>=10.0

# ---- Visualization / dashboard ----
matplotlib>=3.8
seaborn>=0.13
gradio>=4.44,<6.0
