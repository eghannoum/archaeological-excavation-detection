# @package _global_
# ── YOLO26 xlarge ─────────────────────────────────────────
# Batch size: 4 (fits 8 GB VRAM on RTX 5070 Laptop)

experiment:
  name: yolo26x
  description: "YOLO26 xlarge — 113.2M params, maximum accuracy"

model:
  name: yolo26x
  variant: xlarge

training:
  batch_size: 4
