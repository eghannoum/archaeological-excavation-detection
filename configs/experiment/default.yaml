# Experiment metadata (default — override via experiment=<name>)
# Activatable via: python train.py experiment=yolo26n

name: yolo26n                # default experiment; override via experiment=<name>
description: null
tags: []
notes: null
log_level: INFO
log_frequency: 50            # log every N steps
save_frequency: 10           # save checkpoint every N epochs
